<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 구매자 -->  
<mapper namespace="Buy">

<!-- 내 후원내역 -->
<select id="myOrderList" parameterType="donzo.thefun.model.BuyDto" resultType="donzo.thefun.model.BuyDto">
		SELECT SEQ, ID, PROJECTSEQ, OPTIONSEQ, COUNT, PRICE, REGDATE, SCORE, BCOMMENT, PTITLE, OTITLE, OCONTENT, STATUS
		FROM FUN_BUY_VIEW
		WHERE ID=#{id}
</select>

<!-- add Order -->
<insert id="addOrders" parameterType="donzo.thefun.model.BuyDto">
	INSERT INTO FUN_BUY
	VALUES(SEQ_BUY.NEXTVAL,#{id},#{projectseq},#{optionseq},#{count},#{price},SYSDATE,null,null)
</insert>

</mapper>